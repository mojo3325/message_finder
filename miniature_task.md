# Задача: Добавить поддержку медиафайлов в миниатюру чата

## Контекст и цель

Текущая реализация миниатюры чата в `warp_chat.py` отображает только текстовые сообщения. Нужно расширить функциональность, чтобы поддерживать отображение изображений, голосовых сообщений и других медиафайлов в текстовом формате.

## Текущее состояние

### Структура миниатюры
- Миниатюра создается в функции `build_warp_miniature()` в `tg/ui.py`
- Сообщения передаются в формате списка словарей с ключами: `direction`, `author`, `text`
- В цикле (строки 176-187) обрабатываются только сообщения с непустым текстом
- Если `text` пустой, сообщение пропускается (строка 180-181)

### Сбор данных сообщений
- Основная логика в `_collect_warp_dialog_state()` в `tg/handlers/warp.py`
- Доступ к объекту сообщения `mm` (Telethon Message)
- Сейчас извлекается только `raw = getattr(mm, "message", None)` для текста
- Структура `preview_buffer` содержит только текстовые сообщения

## Требуемый функционал

### Отображение медиафайлов в миниатюре
1. **Изображения**: `← имя пользователя: изображение.jpg (формат изображения)`
2. **Голосовые сообщения**: `← имя пользователя: голосовое сообщение`
3. **Аудио**: `← имя пользователя: аудио сообщение`
4. **Видео**: `← имя пользователя: видео.mp4 (формат видео)`
5. **Документы**: `← имя пользователя: документ.pdf (формат файла)`
6. **Стикеры**: `← имя пользователя: стикер`
7. **GIF**: `← имя пользователя: GIF анимация`
8. **Файлы**: `← имя пользователя: файл.pdf (формат файла)`
9. **Другие типы медиа**: `← локации на карте`

### Мультимодальный ввод в replier.py
**Основная цель**: Расширить функциональность `services/replier.py` для передачи мультимодальных данных (изображения, голосовые сообщения) в контекст чата, чтобы модель-ответчик могла анализировать не только текст, но и медиафайлы из истории сообщений.

**Суть задачи**: Когда в чате есть сообщения с изображениями или голосовыми сообщениями, они должны передаваться в контекст `context` параметра функции `generate_reply_sync()` в мультимодальном формате, позволяя Gemini анализировать визуальный и аудио контент для более точного понимания контекста беседы.

#### Поддерживаемые типы медиа для анализа
1. **Изображения** - анализ содержимого, описание, извлечение текста (OCR)
2. **Голосовые сообщения** - транскрипция в текст для анализа контекста
3. **Аудиофайлы** - транскрипция и анализ содержимого
4. **Видео** - анализ кадров и аудиодорожки

#### Технические требования
- Использовать OpenAI-совместимый API Gemini с `base_url="https://generativelanguage.googleapis.com/v1beta/openai/"`
- Поддержка структуры сообщений с `content` массивом для мультимодального контента
- Обработка медиафайлов через `base64` кодирование для изображений
- Транскрипция аудио через Gemini API или внешние сервисы
- Fallback на текстовое описание медиа при ошибках обработки
- **Ключевое требование**: Медиафайлы из контекста чата должны передаваться в модель вместе с текстовыми сообщениями

### Приоритет отображения
- Если сообщение содержит медиа, отображать тип медиа вместо текста
- Если сообщение содержит и медиа и текст, показывать оба
- Если медиа без текста, показывать только тип медиа
- Ограничение: максимум 4 последних сообщения в миниатюре

## Технические детали для реализации

### Доступные атрибуты сообщений Telethon
В объекте `mm` доступны следующие атрибуты для определения типа медиа:
- `mm.photo` - фото
- `mm.voice` - голосовое сообщение
- `mm.audio` - аудиофайл
- `mm.video` - видео
- `mm.document` - документ
- `mm.sticker` - стикер
- `mm.gif` - GIF анимация
- `mm.file` - общий файл

### Функции-хелперы для определения типа
Нужно создать вспомогательные функции:
```python
def get_media_type(mm) -> Optional[str]:
    """Возвращает тип медиа или None если текст"""

def get_media_info(mm) -> Optional[Dict[str, str]]:
    """Возвращает информацию о медиа: тип, формат, размер и т.д."""

def process_media_for_gemini(mm) -> Optional[Dict]:
    """Обрабатывает медиа для передачи в Gemini API"""
    
def encode_image_to_base64(image_path: str) -> str:
    """Кодирует изображение в base64 для Gemini API"""
    
def transcribe_audio(audio_path: str) -> str:
    """Транскрибирует аудио в текст через Gemini API"""
```

### Структура обновленного сообщения
```python
{
    "direction": "out" if is_out else "in",
    "author": author,
    "text": text_clean,  # может быть пустым
    "media_type": Optional[str],  # "photo", "voice", "audio", etc.
    "media_info": Optional[Dict[str, str]],  # {"format": "jpg", "size": "1.2 MB"}
    "has_media": bool,
    "media_data": Optional[Dict],  # для передачи в replier.py
    "media_base64": Optional[str],  # base64 для изображений
    "media_file_path": Optional[str]  # путь к файлу для обработки
}
```

### Структура мультимодального сообщения для Gemini API
```python
{
    "role": "user",
    "content": [
        {"type": "text", "text": "Описание изображения:"},
        {
            "type": "image_url",
            "image_url": {
                "url": "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQ..."
            }
        }
    ]
}
```

### Обновленная структура контекста с медиа
```python
# Вместо простого текстового контекста:
context = "← Иван: Привет\n← Мария: Как дела?\n→ Вы: Хорошо"

# Будет мультимодальный контекст:
context_messages = [
    {
        "role": "user", 
        "content": [
            {"type": "text", "text": "← Иван: Привет"}
        ]
    },
    {
        "role": "user",
        "content": [
            {"type": "text", "text": "← Мария: Посмотри на этот график"},
            {
                "type": "image_url",
                "image_url": {"url": "data:image/jpeg;base64,/9j/4AAQ..."}
            }
        ]
    },
    {
        "role": "user",
        "content": [
            {"type": "text", "text": "← Мария: [голосовое сообщение: 'Что думаешь?']"}
        ]
    }
]
```

## Высокоуровневый план задач

### 1. Анализ структуры сообщений Telethon
- Изучить доступные атрибуты для медиафайлов
- Определить логику приоритета типов медиа
- Создать функции-хелперы для работы с медиа

### 2. Модификация сбора данных сообщений
- Обновить `_collect_warp_dialog_state()` для извлечения медиа-информации
- Обновить `_fetch_private_dialogs_with_preview()` для предварительного просмотра
- Изменить структуру данных для включения информации о медиа

### 3. Обновление рендера миниатюры
- Модифицировать `build_warp_miniature()` для отображения медиа
- Добавить логику форматирования разных типов медиа
- Обеспечить корректное отображение смешанного контента

### 4. Мультимодальный ввод в replier.py
- Обновить `generate_reply_sync()` и `generate_reply()` для поддержки медиа
- Добавить функции обработки изображений (base64 кодирование)
- Реализовать транскрипцию аудио (голосовые сообщения)
- Создать функции конвертации медиа в формат Gemini API
- Добавить обработку ошибок для медиафайлов
- **Ключевая задача**: Модифицировать передачу контекста так, чтобы медиафайлы из истории чата передавались в модель вместе с текстом

### 5. Интеграция с Gemini API
- Обновить структуру сообщений для мультимодального контента
- Реализовать передачу изображений через `image_url` с base64
- Добавить поддержку аудио транскрипции через Gemini
- Обеспечить fallback на текстовое описание при ошибках

### 6. Тестирование и валидация
- Проверить отображение различных типов медиа в миниатюре
- Протестировать анализ изображений через Gemini
- Проверить транскрипцию голосовых сообщений
- Убедиться в корректной работе с сообщениями без медиа
- Протестировать крайние случаи (несколько медиа подряд)

## Ожидаемый результат

### Миниатюра чата
После реализации миниатюра чата будет показывать:
```
📩 Чат с Иваном Ивановым · 14:30
┌─ Чат с Иваном Ивановым ───────────────────────
← Иван Иванов:
   🖼 Фото.jpg
← Вы:
   🎙 Голосовое сообщение 0:02
← Иван Иванов:
   📄 Документ.pdf
→ Вы:
   Привет! Как дела?

└──────────────────────────────────────────────────
[Перегенерировать] [Назад] [Отправить]
```

### Мультимодальный анализ в replier.py
Gemini сможет анализировать медиафайлы в контексте чата:

**Пример 1: Анализ изображения в контексте**
```
Контекст чата:
← Иван: Привет! Посмотри на этот график
← Иван: [изображение с графиком продаж]
← Иван: Что думаешь о тренде?

Текущее сообщение: "Можешь объяснить детальнее?"

Gemini получает:
- Текстовые сообщения из контекста
- Изображение с графиком (base64)
- Может проанализировать график и дать детальный ответ о тренде
```

**Пример 2: Голосовые сообщения в контексте**
```
Контекст чата:
← Мария: [голосовое сообщение: "У меня проблема с проектом"]
← Мария: [голосовое сообщение: "Нужна помощь с кодом"]

Текущее сообщение: "Как помочь?"

Gemini получает:
- Транскрибированный текст голосовых сообщений
- Понимает, что нужна помощь с кодом проекта
- Может предложить конкретные решения
```

**Пример 3: Смешанный контент в контексте**
```
Контекст чата:
← Петр: Вот скриншот ошибки
← Петр: [изображение с ошибкой в консоли]
← Петр: [голосовое сообщение: "Не понимаю что делать"]

Текущее сообщение: "Помоги разобраться"

Gemini получает:
- Изображение с ошибкой (может прочитать текст ошибки)
- Транскрибированный голос
- Может дать точный ответ на основе визуальной информации
```

## Файлы для изменения
1. `/tg/handlers/warp.py` - логика сбора данных
2. `/tg/ui.py` - рендер миниатюры
3. `/services/replier.py` - мультимодальный ввод для Gemini
4. `/services/clients.py` - возможно, обновление клиента Gemini
5. `/tg/helpers.py` - вспомогательные функции для медиа
6. `/core/types.py` - возможно, новые типы данных

## ТЕХНИЧЕСКИЕ ДЕТАЛИ
- Использовать Telethon API для доступа к атрибутам сообщений
- Сохранить совместимость с существующими текстовыми сообщениями
- Обработать случаи когда медиа без текста
- Обработать случаи когда есть и медиа и текст
- Использовать OpenAI-совместимый Gemini API для мультимодального ввода
- Реализовать base64 кодирование для изображений
- Добавить транскрипцию аудио через Gemini API
- Обеспечить fallback на текстовое описание при ошибках обработки медиа

## КРИТЕРИИ УСПЕХА
✅ Миниатюра показывает медиафайлы в текстовом формате
✅ Текстовые сообщения продолжают работать как раньше
✅ Смешанный контент (медиа + текст) отображается корректно
✅ Нет ошибок при обработке сообщений без медиа
✅ Ограничение в 4 последних сообщения сохраняется
✅ **НОВОЕ**: Gemini может анализировать изображения в контексте
✅ **НОВОЕ**: Голосовые сообщения транскрибируются и анализируются
✅ **НОВОЕ**: Мультимодальный контент корректно передается в API
✅ **НОВОЕ**: Fallback работает при ошибках обработки медиа
