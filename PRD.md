## PRD — Скрипт-классификатор сообщений Telegram (Docker + Cerebras SDK)

### 1. Цель
- **Что**: Непрерывно работающий Python-скрипт в Docker, мониторящий новые сообщения во всех доступных общих чатах Telegram-аккаунта, выполняющий бинарную классификацию (0/1) каждого текстового сообщения через модель `qwen-3-235b-a22b-instruct-2507` (Cerebras API, официальный Python SDK).
- **Зачем**: Автоматически находить «вопросы новичков» и отправлять персональные уведомления через Telegram-бота только тем авторам сообщений, которые предварительно запустили бота командой `/start`.

### 2. Ключевая ценность
- **Моментальная фильтрация** входящих сообщений по критерию «вопрос новичка».
- **Нулевой шум**: модель обязана возвращать строго `0` или `1`.
- **Мгновенное уведомление** при детекции, с полной метаинформацией и ссылкой на исходное сообщение.

### 3. Область применения
- Личный Telegram-аккаунт; только общие чаты: групповые, супер-группы и каналы с комментариями. Личные чаты (DM) исключены.
- Сообщения: только текст. Медиа/вложения и голосовые не анализируются (игнорируются).

### 4. Основные сценарии
- **S1 — Мониторинг**: Скрипт слушает новые сообщения только из групп/супергрупп/каналов (без личных диалогов).
- **S2 — Классификация**: На каждое новое текстовое сообщение вызывается Cerebras SDK с системным промптом, жёстко ограничивающим ответ `0` или `1`.
- **S3 — Проверка подписки**: Если ответ `0` → пропуск. Если `1` → проверить, запускал ли автор сообщения бота (`/start`) и есть ли его `user_id` в реестре подписчиков бота.
- **S4 — Уведомление**: Если автор сообщения подписан (запускал бота `/start` и не отписывался), бот отправляет ЛС только автору: имя, никнейм, название чата, подчеркнутый исходный текст сообщения, прямая ссылка на сообщение. Если автор не подписан — пропуск без попытки связаться.

-### 5. Функциональные требования
- **F1. Телеграм-клиент (аккаунт)**: Авторизация и подписка на новые сообщения в общих чатах (groups/supergroups/channels), без личных диалогов.
- **F2. Очередь обработки**: Асинхронная обработка сообщений без блокировки потока получения.
- **F3. Вызов модели**: Использовать официальный Python SDK Cerebras и модель `qwen-3-235b-a22b-instruct-2507`.
- **F4. Строгий контракт модели**: Ответ модели — один символ `0` или `1` (без пробелов/комментариев). Любое иное — трактуется как ошибка вызова.
- **F5. Логика решения**: `0` → no-op; `1` → продолжить к проверке подписки.
- **F6. Реестр подписчиков бота**: Вести список `user_id`, которые отправили `/start` боту (через Bot API апдейты). Использовать только для проверки права на уведомление. Команда `/stop` удаляет пользователя из реестра (отписка).
- **F7. Уведомления ботом**: Бот отправляет ЛС только автору сообщения и только если он присутствует в реестре подписчиков. Нельзя начинать диалог от лица бота в обход `/start`. Команда `/stop` немедленно прекращает рассылку для пользователя.
- **F8. Формат уведомления**: 
  - Имя: `<first_name last_name>` (если доступно)
  - Никнейм: `@username` (если доступно)
  - Чат: `<title | type>`
  - Сообщение: подчеркнутый оригинальный текст (использовать поддерживаемый Telegram формат)
  - Ссылка: прямой `t.me/c/.../...` или `https://t.me/<username>/<message_id>` при доступности
- **F8.1. Генерация ответа**: В уведомлении присутствует кнопка `⚡ Сгенерировать ответ`. При нажатии:
  - Текст уведомления подменяется на блок `✍️ Предложенный ответ` с 1–3 фразами, подталкивающими к диалогу (модель `qwen-3-235b-a22b-instruct-2507`, Cerebras SDK).
  - Кнопки меняются на: `⬅ Назад` (вернуть исходное уведомление) и `🔁 Перегенерировать` (повторная генерация).
  - Дополнительно отображается кнопка `📋 Скопировать` (использует `copy_text`, если поддерживается клиентом Telegram; иначе — отсутствует по ограничению платформы).
- **F9. Повторные попытки**: При временных сбоях (сеть, 5xx) — экспоненциальный backoff.
- **F10. Идемпотентность**: Не дублировать уведомления при повторной обработке одного и того же сообщения.
- **F11. Фильтр контента**: Классифицировать только текст; медиа/голосовые игнорировать.
- **F12. Персистентная сессия**: Сохранять Telegram-сессию (StringSession или файл) через ENV/volume, чтобы не вводить код/облачный пароль при каждом запуске.

### 6. Нефункциональные требования
- **N1. Среда**: Обязательный запуск в Docker. Один контейнер, одна служба.
- **N2. Производительность**: Целевой p95 latency классификации ≤ 1.5s при нормальной сети.
  - Для генерации ответа целевой p95 ≤ 2.0s.
- **N3. Надёжность**: Автовосстановление при падениях; не терять непрочитанные апдейты (использовать оффсет/идемпотентность).
- **N4. Масштабируемость**: Возможность горизонтального шардирования по аккаунтам (в будущем). В V1 — один аккаунт.
- **N5. Безопасность**: Секреты только через переменные окружения/secret-хранилище.
- **N6. Наблюдаемость**: Структурированные логи, метрики: количество сообщений/мин, доля `1`, ошибки SDK/API.
 - **N7. Лимиты Cerebras**: Соблюдать квоты модели `qwen-3-235b-a22b-instruct-2507` — Requests: 30/мин, 900/час, 14 400/день; Tokens: 60 000/мин, 1 000 000/час, 1 000 000/день. В случае превышения — очереди/бекпрешер, задержки и повтор через `Retry-After`.

### 7. Ограничения и предположения
- **O1**: Используется официальный Python SDK Cerebras (актуальные эндпоинты/лимиты — по документации).
- **O2**: Телеграм-клиент — официальный API (MTProto) через выбранную Python-библиотеку.
- **O3**: В V1 не обрабатываются голосовые/медиа без текста.
- **O4**: Требуется действующий аккаунт Telegram, API ID/Hash, сессия хранится безопасно.

### 8. Входы/выходы
- **Вход**: Новые сообщения Telegram (текст).
- **Выход (внутренний)**: Решение модели `0/1`.
- **Выход (внешний)**: Уведомление пользователю при `1`.

### 9. Границы качества
- **Ошибки модели**: Любой невалидный ответ → лог + retry с ограничением; при повторной невалидности — пропуск.
- **Локализация**: Сообщения могут быть на любом языке; промпт должен быть языконейтральным.

### 10. Деплой и конфигурация
- **Docker-only** запуск; конфиг через ENV:
  - `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_STRING_SESSION` (или путь к .session и подключённый volume)
  - `TELEGRAM_BOT_TOKEN` (токен бота для отправки уведомлений через Bot API)
  - `CEREBRAS_API_KEY`
  - `RATE_LIMIT_RPM` (default: 30), `RATE_LIMIT_RPH` (default: 900)
  - `RATE_LIMIT_TPM` (default: 60000)
  - Доп.: `LOG_LEVEL`, `REQUEST_TIMEOUT_S`, `RETRY_MAX_ATTEMPTS`

### 11. Критерии приёмки
- Скрипт стабильно запускается в Docker и слушает все общие чаты.
- На тестовом наборе сообщений модель возвращает строго `0` или `1`.
- При метке `1` и наличии автора в реестре подписчиков бот отправляет корректное ЛС с требуемыми полями и кликабельной ссылкой.
- При метке `1`, но отсутствии подписки автора, уведомление не отправляется (никаких ошибок/ретраев на отправку).
- Нет дубликатов уведомлений при повторе апдейтов.
- Личные сообщения не обрабатываются.
- Сообщения без текста (медиа/голосовые без подписи) игнорируются и не отправляются в модель.
 - При нагрузке выше квот Cerebras запросы не падают массово: лишние ставятся в очередь, обрабатываются c задержкой и/или отклоняются с понятным логом и метрикой.
