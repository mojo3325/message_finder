## Warp Chat — список приватных чатов и миниатюра диалога

### Цель
Реализовать механику, при которой бот `warp_chat` показывает пользователю список ВСЕХ его приватных диалогов (не группы/каналы). При выборе чата текущее сообщение заменяется «миниатюрой» диалога (последние 4 сообщения) с кнопками управления. По нажатию «Сгенерировать» — миниатюра переходит в ui состояние загрузки, после того как будет сгенерирован ответ, миниатюра возвращается и должна быть дополнена новым сгенерированным сообщением(сообщение должно быть подчеркнутым и напечатано курсивом), кнопка сгенерировать меняется на «Перегенерировать», помимо кнопки сгенерировать должна быть кнопка назад, которая возвращает пользователя к списку чатов, а так же кнопка отправить.

### Ограничения и предпосылки
- Для чтения списка чатов, истории и отправки сообщений «от имени пользователя» используется Telethon-клиент пользователя (string session). При отсутствии активной сессии — показать дружелюбное сообщение о необходимости привязки и скрыть действия, требующие сессии.

### Хранилище
- Долговременное хранение списка чатов не требуется(список чатов должен быть получен только при вызове команды `/account` и храниться в памяти для быстрого переключения ui). Список приватных чатов получается on-demand через Telethon (`get_dialogs` с фильтром private, опционально исключая ботов).
- Кэш индексации контекста (in-memory) по ключу `(telegram_user_id, chat_id)`:
```json
{
  "<telegram_user_id>__<chat_id>": {
    "indexed_at": "<ISO8601>",
    "limit": 40,
    "message_ids": [<int>],
    "sample": "<обрезанный фрагмент>",
    "transcript": "<ПОЛНЫЙ размеченный транскрипт OWNER/USER по последним N сообщениям>",
    "miniature_last": 4
  }
}
```
- Хранилище драфтов: `services/drafts.py` с полным UI-состоянием (html, ссылки/preview, `author_user_id`, `source_chat_id`, `context_message_ids`). Требование: корректное восстановление предпросмотра и миниатюры для колбэков и редактирования.

### Индексация контекста
- Источник: Telethon от имени пользователя.
- Объём для генерации: последние `N=40` сообщений (конфигурируемо в `config.py`).
- Миниатюра: для UI берутся последние `miniature_last=4` сообщений (конфигурируемо), с тем же форматированием, что и в индексе.
- Результат индексации сохраняется в кэше (см. Хранилище).

### Пользовательский поток
1) Пользователь вызывает команду (например, `/account`)
2) Бот показывает список приватных диалогов (постранично). Элементы списка — «текст-кнопки» с названием чата.
3) Нажатие на чат → текущее сообщение заменяется «миниатюрой» диалога: последние 4 сообщения, под ними кнопки: `[Сгенерировать]`, `[Назад]`, `[Отправить]` (отправка неактивна до появления драфта).
4) Нажатие `[Сгенерировать]`:
   - запускается индексация (N=40) и генерация ответа; показываем прогресс;
   - миниатюра переходит в ui состояние загрузки
   -после того как будет сгенерирован ответ, миниатюра возвращается и должна быть дополнена новым сгенерированным сообщением(сообщение должно быть подчеркнутым и напечатано курсивом)
   - кнопка сгенерировать меняется на «Перегенерировать»
   - помимо кнопки сгенерировать должна быть кнопка назад, которая возвращает пользователя к списку чатов, а так же кнопка отправить.
5) Нажатие `[Перегенерировать]` — повторная генерация с тем же контекстом (обновляется предпросмотр миниатюры).
6) Нажатие `[Назад]` — возврат к списку приватных чатов (редактируется текущее сообщение).
7) Нажатие `[Отправить]` — публикация сгенерированного ответа в выбранный чат (без reply) через Telethon «от имени пользователя».

### Callback протокол (Bot API)
- `list:<page>` — показать список приватных чатов (пагинация).
- `open:<chat_id>` — открыть миниатюру выбранного чата (редактировать текущее сообщение).
- `gen:<chat_id>` — начать генерацию по чату, изменить кнопку на «Перегенерировать».
- `regen:<draft_id>` — перегенерация текущего драфта (обновить предпросмотр).
- `back:chats` — вернуться к списку чатов.
- `send:<draft_id>` — отправка ответа через Telethon в выбранный чат.

### Драфты ответа
- При генерации сохранять драфт в стораже (`services/drafts.py`) по `draft_id` со следующими полями:
```json
{
  "draft_id": "<uuid>",
  "telegram_user_id": <int>,
  "chat_id": <int>,
  "text": "<string>",
  "html": true,
  "author_user_id": <int|null>,
  "source_chat_id": <int>,
  "context_message_ids": [<int>],
  "created_at": "<ISO8601>"
}
```
- Восстановление UI-состояния должно быть полным: html/formatting, ссылки/preview, `author_user_id`, `source_chat_id`, контекст для корректных колбэков и редактирования.

### UI и клавиатуры
- Экран списка чатов:
  - Текст: «Мои приватные чаты (страница X/Y)»
  - Элементы: строки с названиями чатов как текст-кнопки `(open:<chat_id>)`.
  - Кнопки: навигация `⬅ Prev`, `Next ➡`.
- Экран миниатюры чата:
  - формат (реализован в `tg/ui.py(def build_warp_miniature)`):

//миниатюра до генерации

  ┌─ 📩 Чат с Алексей Иванов · 14:30 ──────────────────────────────────
→ <b>Вы:</b>
   Привет! Как дела?
← Алексей Иванов:
   Привет! Хорошо, спасибо. А у тебя?
→ <b>Вы:</b>
   Тоже нормально, спасибо!
← Алексей Иванов:
   Отлично! Чем занимаешься?

└──────────────────────────────────────────────────

//миниатюра предпросмотра после генерации

  ┌─ 📩 Чат с Алексей Иванов · 14:30 ──────────────────────────────────
→ <b>Вы:</b>
   Привет! Как дела?
← Алексей Иванов:
   Привет! Хорошо, спасибо. А у тебя?
→ <b>Вы:</b>
   Тоже нормально, спасибо!
← Алексей Иванов:
   Отлично! Чем занимаешься?
→ <b>Сгенерированный ответ:</b>
   <i><u>Я занимаюсь разработкой бота для Telegram, который генерирует ответы на основе контекста.</u></i>

└──────────────────────────────────────────────────

  - Кнопки: `[Сгенерировать] (gen:<chat_id>)` → после конца генерации меняется на `[Перегенерировать] (regen:<draft_id>)`, `[Назад] (back:chats)`, `[Отправить] (send:<draft_id>|disabled)`.


РЕАЛЬНАЯ СИТУАЦИЯ РЕАЛИЗАЦИИ:

**❌ ПРОБЛЕМЫ В ТЕКУЩЕЙ РЕАЛИЗАЦИИ:**

1. **Неправильные кнопки открытия чата:**
   - При `/account` выводится немедленный ответ с "Индексация чатов..." а потом список чатов,
   возле каждого чата должна быть текстовая кнопка открыть чат, вместо этого кнопки выводятся в виде инлайн клавиатуры
   ИСПРАВЬ ИХ НА ТЕКСТОВЫЕ КНОПКИ В СООБЩЕНИИ

2. **Неправильный предпросмотр:**
   - При нажатии на кнопку генерации, сообщение не меняется на состояния загрузки генерации, вместо
   этого сообщение редактируется на миниатюру с надписью "Генерация ответа..." в последнем сообщении.
   - Не правильно отображается сгенерированный ответ в миниатюре предпросмотра. Вместо "→ Вы:" должно быть "→ Сгенерированный ответ:"
   как в примере выше.

### Алгоритмика обработчиков (текущая, НЕПРАВИЛЬНАЯ)

**❌ ТЕКУЩЕЕ ПОВЕДЕНИЕ (не соответствует задаче):**
1) Команда списка: немедленный ответ "Индексация чатов..." → отправка сообщения со списком чатов с инлайн-кнопками вместо текстовых кнопок в сообщении
2) `gen:<chat_id>`: немедленная смена кнопки → асинхронная генерация → редактирование сообщения на миниатюру с "Генерация ответа..." в конце + неправильное отображение сгенерированного ответа
3) `regen:<draft_id>`: немедленная смена на "Генерация..." → перегенерация → обновление миниатюры

**✅ ПРАВИЛЬНОЕ ПОВЕДЕНИЕ (согласно задаче):**
1) Команда списка /account: немедленный ответ "Индексация чатов..." → асинхронная загрузка → редактирование сообщения на список с текстовыми кнопками чатов в сообщении (не инлайн клавиатура)
2) `open:<chat_id>`: немедленный ответ "Индексирую чат..." → асинхронная загрузка миниатюры → редактирование сообщения на миниатюру
3) `gen:<chat_id>`: немедленная смена сообщения на "Генерация ответа..." → асинхронная генерация → редактирование сообщения на миниатюру предпросмотра с результатом в формате как в примере выше + смена кнопки на "Перегенерировать"
4) `regen:<draft_id>`: немедленная смена на "Генерация..." → перегенерация → обновление предпросмотра


### Ошибки и крайние случаи
- Нет Telethon-сессии пользователя → информировать и предложить привязку; скрыть действия, требующие сессии.
- Недоступный чат/запрещён доступ → сообщить об ошибке и не падать; логировать причину.
- Индексация > лимита API → обрезать до N; настраивается в `config.py`.
- Слишком много чатов → пагинация списка.
- `Send` без драфта → кнопка неактивна.

Примечание: Поле `transcript` хранит полный размеченный контекст (строка), например строки вида `OWNER @username: text` / `USER id:123: text` в хронологическом порядке (старые → новые) для последних `N` сообщений. Генератор ответа использует именно `transcript`.

### Метрики и логирование
- Логировать этапы: list_open, open_chat, index_start, index_done, gen_start, gen_done, send_ok, send_fail (с таймингами).
- Счётчики: количество просмотренных чатов, успешных индексаций, успешных отправок.

### Критерии приёмки (текущее состояние НЕ ПРОХОДИТ)

**❌ ТЕКУЩЕЕ СОСТОЯНИЕ (неприемлемо):**
- Список чатов выводится с инлайн-кнопками вместо текстовых кнопок в сообщении ❌
- При генерации нет состояния загрузки, сгенерированный ответ показывается неправильно ❌
- Сгенерированный ответ отображается как "→ Вы:" вместо "→ Сгенерированный ответ:" ❌

**✅ ПРАВИЛЬНЫЕ КРИТЕРИИ ПРИЕМКИ:**
- При `/account` список чатов показывается с текстовыми кнопками в сообщении (не инлайн клавиатура)
- При генерации сообщение меняется на "Генерация ответа..." и кнопка меняется ПОСЛЕ завершения
- Результат в миниатюре предпросмотра: миниатюра + сгенерированный ответ в формате "→ Сгенерированный ответ:"
- Кнопки: `[Назад]`, `[Перегенерировать]`, `[Отправить]`

**КРИТИЧНЫЕ ПРОБЛЕМЫ НЕ ИСПРАВЛЕНЫ!** Нужно исправить формат кнопок и отображение сгенерированного ответа.

### TODO: Исправления для Warp Chat

**🔧 КРИТИЧНЫЕ ИСПРАВЛЕНИЯ (приоритетные):**
- [x] **Текстовые кнопки чатов** - заменить инлайн клавиатуру на текстовые кнопки в сообщении для списка чатов
- [x] **Правильный формат сгенерированного ответа** - отображать сгенерированный ответ как "→ Сгенерированный ответ:" вместо "→ Вы:"
- [x] **Улучшенное состояние загрузки** - корректное отображение "Генерация ответа..." при генерации

**✅ Базовый функционал (работает):**
- [x] Получение списка приватных чатов через Telethon
- [x] Обработчик `open:<chat_id>` и рендер миниатюры
- [x] Базовая генерация ответа через `gen:<chat_id>`
- [x] Перегенерация по `regen:<draft_id>`
- [x] Отправка ответа `send:<draft_id>` от имени пользователя
- [x] Обработка отсутствия Telethon-сессии

**⚠️ Проблемы с UI:**
- Инлайн клавиатура вместо текстовых кнопок в списке чатов
- Неправильное форматирование сгенерированного ответа в миниатюре
- Отсутствие четкого состояния загрузки при генерации

**🎯 ЦЕЛЬ:** Исправить критические проблемы с отображением кнопок и форматированием сгенерированного ответа.
